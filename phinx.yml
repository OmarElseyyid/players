paths:
    migrations: migrations

environments:
    default_migration_table: migrations
    default_database: players_db
    players_db:
        adapter: mysql
        host: localhost
        name: players
        user: root
        pass: 
        port: 3306
        charset: utf8mb4

